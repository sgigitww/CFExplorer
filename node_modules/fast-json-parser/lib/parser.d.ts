declare const enum Mode {
    Value = 0,
    Key = 1,
    PrimitiveKey = 2,
    PrimitiveString = 3,
    PrimitiveNumber = 4,
    PrimitiveOther = 5,
    Colon = 6,
    Separator = 7
}
export declare class Parser {
    private numParser;
    private constParser;
    private stringParser;
    private mode;
    private stack;
    private keys;
    value: any;
    init(str?: string, k?: number): void;
    static parse(s: string): any;
    static parseStream<T>(stream: {
        on(event: string, fn: Function): any;
    }): Promise<T>;
    push(str: string): void;
    setMode(mode: Mode): void;
    advance(str: string, k: number): void;
    private parseValue;
    private open;
    private close;
    private parseKey;
    private parseColon;
    private parseSeparator;
    private parsePrimitiveKeyStr;
    private addToContainer;
    private parsePrimitiveStr;
    private parsePrimitiveNum;
    private parsePrimitiveOther;
}
export {};
