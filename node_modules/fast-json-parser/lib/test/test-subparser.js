"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function test(data, np, defaultStart) {
    if (defaultStart === void 0) { defaultStart = 0; }
    np.init(data[0], defaultStart);
    for (var i = 0; i < data.length; ++i) {
        var str = data[i];
        var start = i == 0 ? defaultStart + 1 : 0;
        for (var j = start; j < str.length; ++j) {
            if (np.advance(str, j)) {
                return np.value();
            }
        }
    }
    return np.value();
}
exports.test = test;
//# sourceMappingURL=test-subparser.js.map