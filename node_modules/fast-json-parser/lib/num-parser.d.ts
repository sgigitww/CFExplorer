export declare class NumParser {
    private isNegative;
    private hasExponent;
    private mode;
    private whole;
    private reminder;
    private exponent;
    private divisor;
    private fallbackCharcodes;
    private fallbackPosition;
    init(code: number): void;
    advance(str: string, k: number): boolean;
    private fallback;
    value(): number;
}
